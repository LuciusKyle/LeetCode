
#include <fstream>
#include <iostream>

class Solution {
 public:
  int hammingWeight(uint32_t n) {}
};

using std::cout;
using std::endl;

int main(int argc, char const *argv[]) {
  //std::ofstream file("out.file");
  for(size_t i = 0;i<16;++i){
    cout<<std::hex<<(1<<i)<<endl;
  }


  return 0;
}
