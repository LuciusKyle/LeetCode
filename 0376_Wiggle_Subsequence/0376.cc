
#include <vector>

using std::vector;

class Solution {
 public:
  int wiggleMaxLength(vector<int>& nums) {}

 private:
  int wiggleMaxLength(const int* nums, size_t length) {
    
  }
};

int main(void) {
  Solution sln;
  return 0;
}
