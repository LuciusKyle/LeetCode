
#include<string>
#include<vector>
#include<limits.h>

using std::string;
using std::vector;

class Solution {
 public:
  vector<string> fullJustify(const vector<string>& words, int maxWidth) {
    vector<size_t> costs(words.size(), INT_MAX);
  }
};

int main(int argc, char const* argv[]) {
  Solution sln;

  return 0;
}

